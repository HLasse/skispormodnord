# GPX Webapp

This is a client-side webapp that renders a multi-page PDF from a GPX track.

## Run locally

Use any static server so ES module imports work:

```bash
python3 -m http.server 5173
```

Then open `http://localhost:5173/webapp/`.
